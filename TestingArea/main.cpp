#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
#include <cmath>
#include <numeric>
using namespace std;

int main() {
	cin.tie(NULL);
	cout.tie(NULL);
	ios::sync_with_stdio(false);

	int a, b, c, d, e;
	cin >> a >> b >> c >> d >> e;
	cout << 6 * a + 3 * b + 2 * c + 1 * d + 2 * e << ' ';
	cin >> a >> b >> c >> d >> e;
	cout << 6 * a + 3 * b + 2 * c + 1 * d + 2 * e << ' ';
}